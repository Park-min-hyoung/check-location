import { ToastContainerProps } from "react-toastify";

export const defaultToastifyOptions: ToastContainerProps = {
  position: "top-center",
  limit: 1,
  closeButton: false,
  autoClose: 1000,
  hideProgressBar: false
};
